# ──────────────────────────────────────────────────────────────────────────
# YOLOv26s-pose CodeBase — Python dependencies
# Tested reference stack: Python 3.10–3.12, CUDA 12.1, cuDNN 8.9.x
# ──────────────────────────────────────────────────────────────────────────
#
# IMPORTANT — PyTorch must be installed separately with the correct CUDA build.
# Do NOT rely on the torch line below alone; install via the index URL so the
# CUDA-enabled wheel is selected:
#
#   pip install torch==2.4.0 torchvision==0.19.0 \
#       --index-url https://download.pytorch.org/whl/cu121
#
# For older drivers (CUDA 11.8) use:
#
#   pip install torch==2.4.0 torchvision==0.19.0 \
#       --index-url https://download.pytorch.org/whl/cu118
#
# Then run:
#
#   pip install -r requirements.txt
#
# ──────────────────────────────────────────────────────────────────────────

# Deep learning framework — line kept for reference; install via --index-url above
torch==2.4.0
torchvision==0.19.0

# Ultralytics YOLO (model loading + export API)
ultralytics==8.3.0

# TensorRT Python bindings (pip wheel — simple install path)
# If the pip wheel does not match your CUDA, install from the NVIDIA tarball:
# https://developer.nvidia.com/tensorrt/download/8x
tensorrt>=10.0.0

# ONNX graph tooling — required only by export_tensorrt.py for the .pt → .engine step
onnx==1.16.1
onnx-graphsurgeon>=0.3.27

# Computer vision stack
opencv-python==4.10.0.84
numpy==1.26.4

# Helpful extras pulled in by ultralytics but pinned for reproducibility
pillow>=10.0.0
matplotlib>=3.7.0
tqdm>=4.66.0
pyyaml>=6.0
pandas>=2.0.0
seaborn>=0.13.0
psutil>=5.9.0
py-cpuinfo>=9.0.0
